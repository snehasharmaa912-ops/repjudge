# Copy this file's contents into Streamlit Cloud's "Secrets" settings panel,
# or save it locally as .streamlit/secrets.toml (which is gitignored).
# Never commit your real key.

GEMINI_API_KEY = "your-gemini-api-key-here"
